name: weather_dbt
version: "1.0.0"
config-version: 2

profile: weather_dbt

model-paths: ["models"]

models:
  weather_dbt:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: mart
      +materialized: table